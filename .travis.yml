language: go

go:
  - "1.8"

services:
  - postgresql

env:
  - TEST_DB_NAME=starttls_test

before_script:
  - psql -c 'create database starttls_test;' -U postgres
  - psql starttls_test < db/scripts/init_db.sql

install:
  - go get -u github.com/golang/lint/golint
  - go get ./...

script:
  - golint -set_exit_status ./...
  - go test -v .
  - go test -v ./policy
